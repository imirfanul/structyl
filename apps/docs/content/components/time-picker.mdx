---
title: TimePicker
description: MUI-style time picker with an analog clock panel.
---

# TimePicker

MUI-style time picker with a 12-hour analog clock panel by default, explicit AM/PM selection, optional seconds clock, min/max validation, time steps and segmented primitives.

## Installation

```bash
npx aura-ui add time-picker
```

## Usage

```tsx
import { TimePicker } from '@aura-ui/styled';

export function Example() {
  return (
    <TimePicker
      label="Start time"
      value={time}
      onChange={setTime}
      ampm
      minutesStep={15}
      minTime={new Date(2026, 4, 23, 8, 0)}
      maxTime={new Date(2026, 4, 23, 18, 0)}
    />
  );
}
```

## API Reference

See the [primitive types](https://github.com/your-org/aura-ui/tree/main/packages/primitives/src/time-picker) for the full prop surface. Styled wrappers extend the primitive types with `className` and theme-aware styles.

## Accessibility

Follows the relevant [WAI-ARIA APG pattern](https://www.w3.org/WAI/ARIA/apg/patterns/). The styled picker uses clock number buttons, digital hour/minute view switches, AM/PM buttons and Cancel/OK actions. The compound segment primitive remains available for custom keyboard-first layouts.

## Theming

Styles consume CSS variables from `@aura-ui/themes` (`--bg`, `--fg`, `--primary`, `--border`, etc.). Wrap your app in `<ThemeProvider>` to enable runtime theme switching.
