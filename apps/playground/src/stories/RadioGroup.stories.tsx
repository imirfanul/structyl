import type { Meta, StoryObj } from '@storybook/react';
import { RadioGroup } from '@aura-ui/styled';

const meta: Meta = { title: 'Form/RadioGroup', tags: ['autodocs'] };
export default meta;
type Story = StoryObj;
export const Default: Story = { render: () => <div>RadioGroup story — see playground for full demo</div> };
