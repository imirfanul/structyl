import type { Meta, StoryObj } from '@storybook/react';
import { PasswordToggleField } from '@your-lib/styled';

const meta: Meta = { title: 'Specialty/PasswordToggleField', tags: ['autodocs'] };
export default meta;
type Story = StoryObj;
export const Default: Story = { render: () => <div>PasswordToggleField story — see playground for full demo</div> };
