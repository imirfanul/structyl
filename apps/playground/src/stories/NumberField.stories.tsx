import type { Meta, StoryObj } from '@storybook/react';
import { NumberField } from '@aura-ui/styled';

const meta: Meta = { title: 'Specialty/NumberField', tags: ['autodocs'] };
export default meta;
type Story = StoryObj;
export const Default: Story = { render: () => <div>NumberField story — see playground for full demo</div> };
