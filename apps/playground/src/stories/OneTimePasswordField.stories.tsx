import type { Meta, StoryObj } from '@storybook/react';
import { OneTimePasswordField } from '@your-lib/styled';

const meta: Meta = { title: 'Specialty/OneTimePasswordField', tags: ['autodocs'] };
export default meta;
type Story = StoryObj;
export const Default: Story = { render: () => <div>OneTimePasswordField story — see playground for full demo</div> };
