'use client';

import * as React from 'react';
import type { Meta } from '@storybook/react';
import { Slider } from './index';

const meta: Meta = { title: 'Styled/Slider' };
export default meta;

export const Default = () => <Slider />;
