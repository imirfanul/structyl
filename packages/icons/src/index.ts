/**
 * @structyl/icons
 *
 * Icon set for the library. Currently a thin re-export of lucide-react;
 * may swap to custom SVGs in the future without breaking the API.
 */

export * from 'lucide-react';
