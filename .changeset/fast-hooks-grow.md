---
'@structyl/hooks': minor
---

Add six new SSR-safe hooks: `useIntersectionObserver`, `useResizeObserver`, `useScrollLock`, `useTimeout`, `useInterval`, and `useStep` (a multi-step state machine for wizards, steppers, and carousels).
